#include <iostream>
#include <fstream>
#include <vector>

using namespace std;

bool isAuthorized(string password, string securitylevel) {
	// make sure user password and sec level match whats in the file
}

void randomizeFileSecurity() {
	// give the file names in the txt document of dummy files 
}

int main() {
	string securitylevel = "";
	string password = "";
	cout << "Enter your security level (Enter 'public', 'private', 'secret', or 'topsecret')" << endl;
	cin >> securitylevel;
	cout << "Enter the password for your security level" << endl;
	cin >> password;
	
	
}